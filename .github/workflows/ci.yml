name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -e ".[dev]"

    - name: Validate server import (registers runtime tools)
      env:
        XPLAINABLE_API_KEY: "ci-test-key"
      run: |
        python -c "import xplainable_mcp.server; print('Server import OK')"

    - name: Run tests
      env:
        XPLAINABLE_API_KEY: "ci-test-key"
      run: |
        python -m pytest -q
